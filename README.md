# union-closed-constant

Campaign (2026-09-08) on Frankl's union-closed sets conjecture: the ceiling of the single-letter entropy method
(0.382885, proved) and a protocol reaching it (c >= 0.38284 certified at the rigor level of Liu 2023's 0.382709).

Start with `RESULT.md`, then `CEILING_THEOREM.md` and `paper/note.tex`. Chronology in `progress.md`.
Code: `code/kernel_game.py` (adversary evaluator; reproduces Liu to 1e-9), certification logs `code/certify_*.log`,
`code/ROBUSTNESS_ideal.md`, `code/INERTIA.md`, `code/CONCAVITY_ideal.md`. Literature transcripts in `literature/`.
